import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const pagina2 = ({navigator}) => {
    return (
        <View>
            <Text>karen</Text>
        </View>
    )
}

export default pagina2

const styles = StyleSheet.create({})
